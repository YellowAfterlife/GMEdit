package parsers;
import tools.Aliases;

/**
 * ...
 * @author YellowAfterlife
 */
class GmlLinter {
	public static function run(source:GmlCode):Bool {
		var q = new GmlReaderExt(source);
	}
	//public static function runFor(editor
}

{
    "configurations": [
        {
            "name":"Launch",
            "type":"node",
            "request": "launch",
            "preLaunchTask": "build", 
            "runtimeExecutable": "${workspaceFolder}/bin/electron.exe",
        }
    ]   
}
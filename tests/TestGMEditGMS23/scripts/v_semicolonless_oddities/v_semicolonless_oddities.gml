///note: should highlight normally (including local variables)
function draw_bolt(t, count, xx, yy) {
    var sw = string_width(count)
}

function array_contains(arr, value) {
    var len = array_length(arr)
    for (var i = 0; i < len; i++) {
        if arr[i] == value then return true
    }
    return false
}
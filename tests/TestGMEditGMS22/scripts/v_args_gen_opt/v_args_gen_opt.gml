/// @param a
/// @param ?b
/// @param c=1
///note: generating JSDoc should keep the consistent format as per above
var a = argument[0];
var b = argument_count > 1 ? argument[1] : undefined;
var c = argument_count > 2 ? argument[2] : 1;
v_args_gen_opt(1);